set $mod Mod4

# Static Gaps
set $gapsize 6

gaps top $gapsize
gaps bottom $gapsize
gaps inner $gapsize
gaps outer $gapsize

bindsym $mod+g gaps outer all toggle 6

# Only enable gaps on a workspace when there is at least one container
smart_gaps on
