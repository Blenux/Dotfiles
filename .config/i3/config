# Main
set $mod Mod4
set $bs bindsym
floating_modifier $mod
font pango:hack 10

#Variables
set $ec exec --no-startup-id
set $ea exec_always --no-startup-id

$ec –no-startup-id xset dpms 333 666

# Autostart programs
$ea dunst
$ea feh --bg-fill --randomize ~/Pictures/Wallpapers/Michal\ Kalisz/Aircraft\ engine/*
$ea picom -b
#$ea kitty

# Main Apps
$bs $mod+v exec vivaldi-stable
$bs $mod+e exec dolphin
$bs $mod+Return exec kitty
$bs $mod+p exec qbittorrent
$bs $mod+F9 exec cctv-viewer
# Coding
$bs $mod+F2 exec gitkraken
$bs $mod+Shift+F2 exec "code ~/.config/i3/config ~/.i3status.conf"
# Compiled Bforartists
$bs $mod+F12 exec /home/juso3d/Documents/Blender-Dev/build_linux/bin/bforartists
$bs $mod+shift+F12 exec /home/juso3d/Documents/Blender-Dev/build_linux_lite/bin/bforartists

# Open applications without a assign workspace to first workspace
assign [class=".*"] $ws1
# Open applications on specific workspaces
# WS1 - Main
assign [class="Vivaldi-stable"] $ws1
assign [class="kitty"] $ws2
assign [class="discord"] $ws1
# WS2 - Code
assign [class="Code"] $ws2
# WS3 - DCC
assign [class="Bforartists"] $ws3
# WS10 - 10
assign [class="qBittorrent"] $ws10
# WS11 - CCTV
assign [class="CCTV Viewer"] $ws11

# kill Apps
$bs $mod+Shift+q kill

# Automatic Switch to Urgent workspace
for_window [class="Vivaldi"] focus
for_window [class="kitty"] focus
for_window [class="qBittorrent"] focus

# Floating
for_window [class="Nitrogen"] floating enable resize set 0 240, move position center
for_window [class="qt5ct"] floating enable resize set 0 240, move position center
#for_window [class="kitty"] floating enable resize set 1024 720, move position center

# Gaps
set $gapsize 2
gaps top $gapsize
gaps bottom $gapsize
gaps inner $gapsize
gaps outer $gapsize

# start dmenu
$bs $mod+d exec --no-startup-id dmenu_run
# alternative menu
$bs $mod+shift+d exec "rofi -modi drun,run -show drun -columns 2 -show-icons"

# change focus:
$bs $mod+Left focus left
$bs $mod+Down focus down
$bs $mod+Up focus up
$bs $mod+Right focus right
# move focus:
$bs $mod+Shift+Left move left
$bs $mod+Shift+Down move down
$bs $mod+Shift+Up move up
$bs $mod+Shift+Right move right

# split orientation
$bs $mod+s split toggle
$bs $mod+shift+s layout toggle split

# enter fullscreen mode for the focused container
$bs $mod+f fullscreen toggle

workspace_layout tabbed 
#<stacking|tabbed>
# change container layout (stacked, tabbed, toggle split)
$bs $mod+w layout toggle tabbed split

# toggle tiling / floating
$bs $mod+Shift+space floating toggle

# change focus between tiling / floating windows
$bs $mod+space focus mode_toggle

# focus the parent container
$bs $mod+a focus parent

# focus the child container
$bs $mod+alt+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1: Main"
set $ws2 "2:Code"
set $ws3 "3: DCC"
set $ws4 "4: Term"
set $ws5 "5: Music"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "CCTV"

# switch to workspace
$bs $mod+1 workspace number $ws1
$bs $mod+2 workspace number $ws2
$bs $mod+3 workspace number $ws3
$bs $mod+4 workspace number $ws4
$bs $mod+5 workspace number $ws5
$bs $mod+6 workspace number $ws6
$bs $mod+7 workspace number $ws7
$bs $mod+8 workspace number $ws8
$bs $mod+9 workspace number $ws9
$bs $mod+0 workspace number $ws10
$bs $mod+ctrl+0 workspace number $ws11

# move focused container to workspace
$bs $mod+Shift+1 move container to workspace number $ws1
$bs $mod+Shift+2 move container to workspace number $ws2
$bs $mod+Shift+3 move container to workspace number $ws3
$bs $mod+Shift+4 move container to workspace number $ws4
$bs $mod+Shift+5 move container to workspace number $ws5
$bs $mod+Shift+6 move container to workspace number $ws6
$bs $mod+Shift+7 move container to workspace number $ws7
$bs $mod+Shift+8 move container to workspace number $ws8
$bs $mod+Shift+9 move container to workspace number $ws9
$bs $mod+Shift+0 move container to workspace number $ws10

# Tab Between Workspaces
#workspace next|prev|next_on_output|prev_on_output
$bs $mod+tab workspace next
$bs $mod+shift+tab workspace prev
# Toggle Between Current and Last Workspaces
#workspace_auto_back_and_forth yes

# Start XDG autostart .desktop files using dex
$ec dex --autostart --environment i3
# Lock and Network
$ec xss-lock --transfer-sleep-lock -- i3lock --nofork
$ec nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
$bs XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
$bs XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
$bs XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
$bs XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
$bs XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5 
$bs XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5 

# resize window (or right mouse click drag)
$bs $mod+r mode "resize"
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # same bindings, but for the arrow keys
        $bs Left resize grow width 10 px or 10 ppt
        $bs Down resize grow height 10 px or 10 ppt
        $bs Up resize shrink height 10 px or 10 ppt
        $bs Right resize shrink width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        $bs Return mode "default"
        $bs Escape mode "default"
        $bs $mod+r mode "default"
}

#title_window_icon toggle
$bs $mod+l title_window_icon toggle
$bs $mod+g gaps inner
#'inner', 'outer', 'horizontal', 'vertical', 'top', 'right', 'bottom', 'left'

#for_window [all] title_window_icon off #Sway Don't Like!

#title_align center
for_window [class="^.*"] border pixel 1
smart_borders on
bindsym $mod+b exec i3-msg border toggle 

# Restart, Reload , Exit:
$bs $mod+Shift+c reload
$bs $mod+Shift+r restart
$bs $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# Coloring
# class                 border  bground text indicator child_border
client.focused          $black $blue2 $white $orange $darkgray
client.focused_inactive $black $black $white $black $black
client.unfocused        $darkred $black $white $black $black
client.urgent           $red $red $white $red $black
client.placeholder      $black $purple $purple $purple $purple

client.background       $white

# i3bar
bar {
        status_command i3status -c ~/.config/i3/i3status.conf
        position top
        separator_symbol "|"

colors 
        {
        background $black
        statusline $white
        separator $red

        focused_workspace  $blue2 $black $white
        active_workspace   $white $black $white
        inactive_workspace $darkred $black $gray
        urgent_workspace   $red $black $white
        }
}

# RGBA Colors
set $red #FF0000
set $green #00FF00
set $blue #0000FF
set $white #FFFFFF
#
set $darkred #AA0000
set $black #000000
set $blue2 #3B6EA5

set $gray #808080
set $darkgray #666666
set $lime #00FF00
set $blue3 #275375
set $purple #800080
set $magenta #FF00FF
set $orange #FFA500
set $cyan #00FFFF
set $dark #778899
set $nord #2f343f
